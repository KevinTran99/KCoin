{
  "name": "my-blockchain-group-assignment",
  "version": "1.0.0",
  "description": "My blockchain group assignment",
  "main": "server.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=config/config.env server.mjs 5001 http://localhost:5001",
    "dev": "nodemon --env-file=config/config.env server.mjs",
    "dev-node": "cross-env GENERATE_NODE_PORT='true' nodemon --env-file=config/config.env server.mjs"
  },
  "author": "Kevin Tran",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "pubnub": "^8.2.5",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@vitest/ui": "^1.6.0",
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.4",
    "vitest": "^1.6.0"
  }
}
